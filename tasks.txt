I ANALYSIS
    1. create a class for df_wBTC
    2. create a class for low/high additional features
II CREATE BOT
    2. write simple transaction with 1inch_wrapper in bot.py
    3. create data fetching for the class
    4. create order giving in the class
    5. try it out with a ganache fork
    6. create tests for daily function
    7. launch on polygon mainnet 

SQL COMMANDS:

postgresql://dimitriandre:89913003Da@localhost:5432/bot_vol

CREATE TABLE cryptos_1inch (
    address     CHAR(42),
    chainId     INT,
    name        VARCHAR(155),
    symbol      VARCHAR(6),
    decimals    INT,
    logoURI     VARCHAR(255),
    PRIMARY KEY (address)
);