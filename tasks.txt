II CREATE BOT
    1. find an address (pk) which has an allowance for 1inch and can spend matic
    2. create the swap transaction
    3. broadcast it on ganache with web3.py
    4. view results

SQL COMMANDS:

postgresql://dimitriandre:89913003Da@localhost:5432/bot_vol

CREATE TABLE cryptos_1inch (
    address     CHAR(42),
    chainId     INT,
    name        VARCHAR(155),
    symbol      VARCHAR(6),
    decimals    INT,
    logoURI     VARCHAR(255),
    PRIMARY KEY (address)
);